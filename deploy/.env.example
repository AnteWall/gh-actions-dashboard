# GitHub webhook secret
# This must match the secret configured in your GitHub repository webhook settings
GITHUB_WEBHOOK_SECRET=your-webhook-secret-here

# Optional: ngrok tunnel for receiving webhooks during development
# Get your authtoken from https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=your-ngrok-authtoken-here

# Optional: Convex self-hosted configuration
# These are only needed if exposing Convex externally
# CONVEX_CLOUD_ORIGIN=https://api.your-domain.com
# CONVEX_SITE_ORIGIN=https://actions.your-domain.com
